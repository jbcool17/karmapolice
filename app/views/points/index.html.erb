<h2>POINTS INDEX</h2>

<% @points.each do |point| %>

	<p> Activity: <%= link_to(Activity.find(point.activity_id).activity, point_path(point.id)) %> || Points From: <%= point.voting_user_id %> || Points Given: <%= point.points %> || Comment: <%= point.comment %></p>
	
<% end %>

<h2>POINTS TOTALS</h2>
<h3>With Users</h3>
<% @point_totals.each do |activity, total| %>

	<p>The user - <%= link_to(User.find(Activity.find(activity).user_id).name, point_path(Point.where(:activity_id => activity).first)) %>, recieved a total of <%= total %> points for their activity of <%= Activity.find(activity).activity %>.</p>

<% end %>