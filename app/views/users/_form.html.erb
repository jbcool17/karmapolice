<div class="user">
<%= form_for(@user) do |f| %>
	<p>
		<%= f.label :email %>
		<%= f.text_field :email %>
	</p>

	<p>
		<%= f.label :name %>
		<%= f.text_field :name %>
	</p>

	<%= f.submit %>
<% end %>

</div>